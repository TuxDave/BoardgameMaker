package it.spaghetticode.bgm.core.game.nodes

import kotlinx.serialization.Serializable

@Serializable
class Folder(): Node(){
//    val isEmpty: Boolean = () -> siblings.isEmpty() todo: know the syntax to do that functions like in python (variable that at each reads it changes the value in base the function)
}